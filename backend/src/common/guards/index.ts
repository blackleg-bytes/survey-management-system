/**
 * Guards Module
 *
 * This module will contain authentication and authorization guards.
 * Examples of guards you might add:
 *
 * - JwtAuthGuard: Validates JWT tokens
 * - RolesGuard: Checks user roles
 * - PermissionsGuard: Checks specific permissions
 * - ThrottlerGuard: Rate limiting per user/route
 *
 * Example usage:
 * @UseGuards(JwtAuthGuard, RolesGuard)
 * @Roles('admin', 'moderator')
 * @Get('admin-only')
 * getAdminData() {}
 */

// Placeholder exports - uncomment and implement as needed
// export * from './jwt-auth.guard.js';
// export * from './roles.guard.js';
// export * from './permissions.guard.js';
